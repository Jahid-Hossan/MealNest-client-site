const SectionTitle = ({ heading }) => {
    return (
        <div className="my-5 lg:my-24">
            <h2 className="text-5xl font-bold">{heading}</h2>
        </div>
    );
};

export default SectionTitle;